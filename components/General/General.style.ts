import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  centeredModalText: {
    textAlign: 'center',
    color: '#000',
    marginBottom: 15,
  },
  bottomButton: {
    marginBottom: -10,
  },
})
